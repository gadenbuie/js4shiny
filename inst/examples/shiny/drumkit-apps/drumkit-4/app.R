library(shiny)

drumkitUI <- function(id) {
  ns <- NS(id)
  tagList(

  )
}

drumkit <- function(input, output, session, ...) {
}

ui <- fluidPage(
)

server <- function(input, output, session) {

}

shinyApp(ui, server)
